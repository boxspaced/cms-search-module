{
    "name": "boxspaced/cms-search-module",
    "require": {

    },
    "minimum-stability": "dev",
    "prefer-stable": true,
    "autoload": {
        "psr-4": {
            "Search\\": "src/"
        }
    },
    "config": {
        "bin-dir": "bin/",
        "process-timeout": 1200
    }
}
